global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'gridai-bot'
    metrics_path: /metrics
    static_configs:
      - targets: ['bot:9000']

rule_files:
  - /etc/prometheus/alerts.yml
